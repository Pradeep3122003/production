---
- name: Pull repository, copy and download files
  hosts: localhost
  tasks:
    - name: Copy file from repository to /tmp
      copy:
        src: "https://github.com/Pradeep3122003/production/blob/master/message.txt"
        dest: "/message.txt"
    
    - name: Run additional Ansible tasks 
      command: service cups status
